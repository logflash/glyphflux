---
"glyphflux": patch
---

Calibrate SDF endpoint alignment against native browser font rasterization at the active device-pixel ratio, preventing visible vertical jumps when controlled morphs and view transitions hand off to settled text.
